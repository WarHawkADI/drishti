[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# LiveKit agent worker — long-running, no HTTP port to bind.
startCommand = "python -m drishti_agent.main start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# No healthcheckPath — the agent makes outbound connections only.
