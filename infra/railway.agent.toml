# Railway service config for the LiveKit Agent worker.

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/agent/Dockerfile"

[deploy]
startCommand = "python -m drishti_agent.main start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# No HTTP healthcheck - agent connects out to LiveKit Cloud.
